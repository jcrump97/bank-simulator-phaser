export default function renderCustomers(rootDocument = document) {
  const container = rootDocument.createElement('div');
  // Placeholder content for customer list rendering
  container.textContent = 'Customer management coming soon.';
  return container;
}
